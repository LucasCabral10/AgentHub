import React from 'react';
import { Heading, Text, Box } from '@chakra-ui/react';

const ClienteInstancias: React.FC = () => {
  return (
    <Box>
      <Heading mb={4}>Instâncias do Cliente</Heading>
      <Text>Aqui são exibidas as instâncias do cliente.</Text>
    </Box>
  );
};

export default ClienteInstancias;
