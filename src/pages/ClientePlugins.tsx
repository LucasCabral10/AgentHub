import React from 'react';
import { Heading, Text, Box } from '@chakra-ui/react';

const ClientePlugins: React.FC = () => {
  return (
    <Box>
      <Heading mb={4}>Plugins do Cliente</Heading>
      <Text>Aqui são exibidos os plugins disponíveis para o cliente.</Text>
    </Box>
  );
};

export default ClientePlugins;
